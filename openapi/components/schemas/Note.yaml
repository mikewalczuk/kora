type: object
required: [id, title, content, created_at, updated_at]
properties:
  id:
    type: string
    format: uuid
  title:
    type: string
  content:
    type: string
    description: Note body in Markdown format
  created_at:
    type: string
    format: date-time
  updated_at:
    type: string
    format: date-time
